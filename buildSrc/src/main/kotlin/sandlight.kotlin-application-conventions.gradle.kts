// For runnable lab modules: common Kotlin setup + the `application` plugin.
// Each module still declares its own mainClass in an application {} block.
plugins {
    id("sandlight.kotlin-common-conventions")
    application
}
